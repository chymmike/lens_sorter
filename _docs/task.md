# Photo Organization Tool - Development Plan

## Phase 1: Skeleton + 基礎設施 ✅
**目標：** 可以執行的空殼程式，有基本 logging

- [x] 建立 `organize_photos.py` 空殼
- [x] 實作 argparse（`--input`, `--output`, `--dry-run`, `--verbose`）
- [x] 實作 `ProgressLogger` class（寫入 `organize.log`）
- [x] 測試：`python organize_photos.py --help` 可以跑

---

## Phase 2: 掃描 + EXIF 讀取 ✅
**目標：** 可以掃描資料夾並讀取 EXIF

- [x] 實作 `scan_files()`：找出所有 JPG/MP4
- [x] 實作 `read_exif()`：用 exiftool 讀取 DateTimeOriginal
- [x] 測試：dry-run 可以列出所有檔案及其拍攝時間

---

## Phase 3: 檔案搬移 + 重新命名 ✅
**目標：** 可以實際搬移並重新命名檔案

- [x] 實作 `generate_new_path()`：計算目標路徑
- [x] 實作 `move_file()`：搬移 + 重新命名
- [x] 處理 sidecar XML（影片的 metadata 檔案）
- [x] 處理檔名衝突（加 `_1`, `_2` 後綴）
- [x] 測試：用 3 張照片測試完整流程

---

## Phase 4: 縮圖產生 ✅
**目標：** 為照片產生縮圖

- [x] 實作 `generate_thumbnail()`：用 sips/Pillow 產生 300px 縮圖
- [x] 加入 `--no-thumbnail` 選項
- [x] 測試：確認縮圖正確產生（25KB from 15MB）正確產生

---

## Phase 5: JSON 索引 ✅
**目標：** 產生 `_index.json`

- [x] 實作 `update_index()`：建立/更新索引
- [x] 索引包含：檔名、路徑、時間、大小、相機型號
- [x] 測試：確認 JSON 格式正確

---

## Phase 6: 互動式事件命名 ✅
**目標：** 處理完後可為日期加上事件名稱

- [x] 實作 `interactive_event_naming()`
- [x] 支援 rename 日期資料夾
- [x] 更新 `_index.json` 的 events 欄位
- [x] 測試：完整流程測試

---

## 當前進度

| Phase | 狀態 |
|-------|------|
| Phase 1 | ✅ 完成 |
| Phase 2 | ✅ 完成 |
| Phase 3 | ✅ 完成 |
| Phase 4 | ✅ 完成 |
| Phase 5 | ✅ 完成 |
| Phase 6 | ✅ 完成 |

**🎉 所有 Phase 完成！**
